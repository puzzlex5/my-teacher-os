(function(root,factory){var api=factory();if(typeof module==='object'&&module.exports){module.exports=api;}else{root.TeacherOSDataTruth=api;}})(typeof globalThis!=='undefined'?globalThis:this,function(){function nextLessonTruth(year,context){var y=year||{},d=context||{};if(d.slot||d.status==='current'||d.status==='next'||d.status==='done')return{known:true,reason:'context-confirmed'};if(d.live===true)return{known:true,reason:'live-week-confirmed'};if(Array.isArray(y.timetable)&&y.timetable.length>0)return{known:true,reason:'basic-timetable-present'};var sync=String((y.comciganSync&&y.comciganSync.status)||'');if(sync==='mismatch')return{known:false,reason:'config-mismatch',label:'컴시간 설정 불일치'};if(sync==='unconfigured')return{known:false,reason:'unconfigured',label:'컴시간 설정 필요'};if(sync==='waiting')return{known:false,reason:'not-verified',label:'컴시간 최신표 미확인'};return{known:false,reason:'no-source',label:'시간표 자료 미확인'};}return{nextLessonTruth:nextLessonTruth};});
