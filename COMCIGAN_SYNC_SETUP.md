# 컴시간 자동동기화

Teacher OS는 `comcigan-config.json`의 설정을 사용해 컴시간 교사용 시간표를 자동 확인합니다.

현재 설정 항목은 다음 세 가지입니다.

- `schoolCode` — 컴시간 학교코드
- `teacherIndex` — 컴시간 교사 선택 번호
- `teacherName` — 교사명(마스킹된 표시도 가능)

GitHub Actions의 `Sync Comcigan timetable` 워크플로가 **매일 오전 09:00 KST**에 실행됩니다. 설정 파일이나 동기화 코드가 바뀌면 최초 확인을 위해 즉시 한 번 더 실행됩니다.

동기화 결과는 `live/comcigan.json`에 저장되고, Teacher OS는 사이트를 열 때 이 파일을 자동으로 읽어 `이번 주 실제 시간표`에 반영합니다. 기본 시간표는 삭제하지 않으며, 컴시간 실제표가 존재하는 주에는 실제표를 우선 사용합니다.

현재 저장소는 개인용으로 운영하므로 별도 암호화 키 없이 동기화하도록 단순화했습니다.

컴시간 데이터는 비공식 `comcigan` 파서를 사용하므로 컴시간 서비스 구조가 변경되면 동기화가 일시적으로 실패할 수 있습니다. 이 경우 기본 시간표와 이미지/문서 일괄 업로드 기능이 fallback으로 남아 있습니다.
